import * as React from 'react'
import Navigation from './Navigation'

const Footer = () => {
  return (
    <footer id={ 'footer' }>
      <h2>{ 'footer' }</h2>
      <Navigation />
    </footer>
  )
}

export default Footer
