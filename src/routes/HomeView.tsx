import * as React from 'react'

export const HomeMeta = () => {
  return (
    <div>{ 'aa' }</div>
  )
}

const HomeView = () => {
  return (
    <div id={ 'home' }>
      <p>{ 'This is Home View.' }</p>
    </div>
  )
}

export default HomeView
